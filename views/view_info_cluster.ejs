<%- include('partials/header') -%>

  <h1>Hello</h1>

  over list
  <div class="userContain">
    <ul>
      top of ul
      <%for (let i=0; i<infoClusterObjectReturn.length; i++){%>
        <li>
          <%= infoClusterObjectReturn[i].Title%>

            ---------------------------
            <%= infoClusterObjectReturn[i].Schema%>__________________
              <%= infoClusterObjectReturn[i].Schema[i]["Field_Name"]%>:::::
                <%= infoClusterObjectReturn[i].Schema[i].Field_Type%>
                  this is the user name(
                  <%= user.userName %>::::
                    <%= user._id %>) testing
                      <%=user.email%>
        </li>
        <%}%>
    </ul>
    <h1>user loop</h1>
    <ul>
      <%for (let i=0; i<user.length; i++){%>
        <li>

          <%= user[i]._id%>

            testing for user
            <%=user[i].email%>
        </li>
        <%}%>
    </ul>
    under list
    <form action="/comment/createComment/" method="POST">
      <input class="" type="text" placeholder="comment" id="comment" name="text">
      <button class="btn btn-primary" type="submit">Post</button>
    </form>
  </div>

  <div class="row justify-content-center mt-5">
    <ul class="row list-unstyled">
      <% for(var i=0; i<infoClusterObjectReturn.length; i++) {%>
        <% if(infoClusterObjectReturn[i].completed === false) {%>
          <li class="col-6 justify-content-between mt-5">
            <a href="/post/<%= infoClusterObjectReturn[i]._id%>">
              Go to this order
            </a>
            <%= infoClusterObjectReturn[i].Name%>
              :::The id for this order is :::
              <%= infoClusterObjectReturn[i]._id%>"
                <span>
                  <i class="fa fa-check-square" aria-hidden="true"></i>
                </span>
                <span>
                  <i class="fa fa-trash" aria-hidden="true"></i>
                </span>


                <form action="/post/likePost/<%= infoClusterObjectReturn[i]._id%>?_method=PUT" method="POST" class="col-1">

                  <button class="btn btn-primary fa fa-check-square" type="submit"></button>
                </form>
                <form action="/post/deletePost/<%= infoClusterObjectReturn[i]._id%>?_method=DELETE" method="POST" class="col-1">

                  <button class="btn btn-primary fa fa-trash" type="submit"></button>
                </form>

          </li>
          <% } %>
            <% } %>
    </ul>
  </div>

  <%- include('partials/footer') -%>